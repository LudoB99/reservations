import { DashLowerCasePipe } from './dash-lower-case.pipe';

describe('DashLowerCasePipe', () => {
  it('create an instance', () => {
    const pipe = new DashLowerCasePipe();
    expect(pipe).toBeTruthy();
  });
});
